<%- include('../templates/layouts/base', {
  pageTitle: 'EJS Test - ' + site.title,
  pageDescription: 'Testing EJS template processing',
  currentPage: 'test',
  content: `
    <section class="hero">
        <div class="hero-content">
            <h1>EJS Template Test</h1>
            <p>Site title: ${site.title}</p>
            <p>Current year: ${currentYear}</p>
            <p>Articles count: ${articles.length}</p>
            <p>Test date formatting: ${formatDate('2025-07-21')}</p>
        </div>
    </section>
    
    <section class="articles-preview">
        <div class="container">
            <h2>Recent Articles</h2>
            <div class="articles-grid">
                ${recentArticles(articles, 3).map(article => `
                    <div class="article-card">
                        <h3><a href="${articleUrl(article.slug)}">${article.title}</a></h3>
                        <p class="article-meta">${formatDate(article.date)} • ${article.category}</p>
                        <p>${truncate(article.description, 100)}</p>
                    </div>
                `).join('')}
            </div>
        </div>
    </section>
  `
}) %>