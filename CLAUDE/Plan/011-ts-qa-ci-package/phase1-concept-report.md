# Plan 011 — Phase 1 Concept-Extraction Report

**Task**: Plan 011 (`ts-qa-ci`), Task 1.1 (research synthesis) and input to Task 1.2 (Opus review pass).
**Sources**: 12 independently-researched sections covering `php-qa-ci` (`/workspace/untracked/repos/php-qa-ci`) and `ec-site` (`/workspace/untracked/ec-site`), merged, de-duplicated, and cross-referenced against this repo (`lts-commerce-site`, `/workspace`) and its `CLAUDE.md`/hooks-daemon configuration.
**Date**: 2026-07-10

## How to read this report

Part A covers `php-qa-ci` — the mature PHP reference — organised around orchestration and delivery philosophy. Part B covers `ec-site` — the TS-native reference that already implements most of this plan's ambition — organised around its ESLint rule catalogue, CDD rules, orchestrator, and meta-tooling. Where two source sections covered the same ground from different angles (e.g. the read-only/CI duality was researched twice, at different depths; several ESLint rules appear in both a batch table and the CDD deep-dive), the fuller/more authoritative treatment is kept and the other is cross-referenced rather than repeated. Part C is the synthesis: for every genuinely open Phase 2 decision (Tasks 2.1–2.7), it states what both references imply and what is still unresolved. Every extracted concept is tagged **TRANSFERS-DIRECTLY**, **NEEDS-TS-ADAPTATION**, or **DOESNT-APPLY**, per the plan's own instruction (Goal 1).

---

## Part A: `php-qa-ci` — Orchestration & Delivery Philosophy

### A.1 Pipeline architecture and phasing philosophy

`php-qa-ci`'s `bin/qa` runs four sequential phases, each a meta-tool invoked through the same `runTool()` dispatch as any leaf tool:

1. **Code Modification** (Rector, PHP CS Fixer) — mutates the tree.
2. **Linting and Validation** (PSR-4, Composer checks, strict types, PHP lint, composer-require-checker, markdown links) — cheap, syntactic/structural.
3. **Static Analysis** (PHPStan, PHPArkitect) — expensive, whole-program type inference.
4. **Testing** (PHPUnit, Infection) — most expensive, only meaningful on type-checked code.

`docs/pipeline.md` states the reasoning directly: *"There is no point running static analysis on code that has not yet been auto-fixed, and no point running tests if the code has syntax errors."* This is a monotonically increasing cost/precision ladder, each phase presupposing the previous succeeded — a textbook fail-fast design, not an arbitrary ordering.

**Why mutation runs first**: if static analysis ran before Rector/CS-Fixer mutated the code, its verdict would describe code that no longer exists by commit time. Running mutation first guarantees every downstream phase analyses the code that will actually ship.

**Composition mechanics worth carrying over regardless of language**:

- A meta-tool (`allCodingStandardsTools`, etc.) is *just another tool* invoked via the same dispatcher as any leaf tool — phases are not a special orchestrator concept, they're ordinary composition.
- **Tool resolution cascade**: `runTool()` resolves each tool name through three tiers — `{project}/qaConfig/tools/{tool}.inc.bash` (project override) → `includes/{platform}/{tool}.inc.bash` (platform-specific) → `includes/generic/{tool}.inc.bash` (fallback) — exclusive, first-match-wins, no merging. A sibling `runNonPlatformTool()` skips the platform tier. (Config *files*, as opposed to tool *scripts*, get the analogous treatment via `configPath()` — see A.4.)
- **Single-tool bypass**: `qa -t <tool>` calls `runTool` directly, skipping phase grouping entirely — useful for iterating on one check without paying for the whole pipeline.
- **Per-tool path-support allowlist**: an explicit `PATH_SUPPORTING_TOOLS` vs `NON_PATH_SUPPORTING_TOOLS` list rejects `-p <path>` against tools that can't honour it (e.g. PHPArkitect, whose paths live in its own config file).
- **One global lock per invocation** (not per-phase) and **two fixed extension points**, `hookPre.bash`/`hookPost.bash` (project-authored, sourced into the pipeline's own shell) — see A.5 for the Claude Code hook system this composes with.

**Fail-fast retry design** (`tryAgainOrAbort()` in `functions.inc.bash`): every leaf tool wraps its invocation in a retry loop. In `CI=true` mode, any failure is immediately fatal. In interactive mode, a failure pauses and prompts "try again? (y/n)" — because a human may fix code in an editor while the terminal waits. A `hasBeenRestarted` flag drives an end-of-run warning: *"RAN WITH RETRIES … you should run the whole process again to be sure everything is fine"*, because a retried tool doesn't re-validate phases that already passed before the fix. Orthogonal to this is a **fail-fast vs aggregate** axis (`qaAggregate`): aggregate mode collects every failing tool in one pass instead of stopping at the first, and only makes sense for a read-only verification run (you can't meaningfully "collect all pending diffs a fixer would apply" while also letting it write). Every leaf tool also distinguishes **crash (exit >1, e.g. bad config/parse failure) from failure (exit 1, a normal rule violation)** — retrying a crash is pointless; retrying a failure after a human fixes something is the whole point of the loop.

**Classification**:

| Concept                                                                                           | Classification                                                                                               |
| ------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| 4-phase cost ladder (mutate → lint → static-analyse → test)                                       | TRANSFERS-DIRECTLY                                                                                           |
| Mutation-first ordering                                                                           | TRANSFERS-DIRECTLY                                                                                           |
| Meta-tool = sequence of guarded tool calls                                                        | TRANSFERS-DIRECTLY                                                                                           |
| Tool-resolution cascade (project → platform → generic)                                            | NEEDS-TS-ADAPTATION (bash `source` has no JS equivalent; needs dynamic `import()`/plugin loader)             |
| Single-tool `-t` bypass; per-tool path-support allowlist                                          | TRANSFERS-DIRECTLY                                                                                           |
| Single global lock; `hookPre`/`hookPost` extension points                                         | TRANSFERS-DIRECTLY                                                                                           |
| CI-vs-interactive retry gate; crash-vs-failure exit-code distinction; fail-fast/aggregate duality | TRANSFERS-DIRECTLY (pure orchestration logic)                                                                |
| `tryAgainOrAbort` mechanism itself                                                                | NEEDS-TS-ADAPTATION (bash `while`+`read` → Node readline/child_process)                                      |
| `hasBeenRestarted` end-of-run warning                                                             | TRANSFERS-DIRECTLY (good UX detail, port verbatim)                                                           |
| Platform detection (Symfony/Laravel)                                                              | DOESNT-APPLY literally; the *resolution-tier pattern* is a plausible analogue for a future Vite/Next.js tier |

ec-site's own `llm:qa` (`npm run llm:type-check && npm run llm:lint && npm run llm:test`) encodes an ordering intuition (type-check → lint → test) but is a flat `&&` chain: no code-mutation phase, no retry loop, no fail-fast/aggregate duality, no phase grouping. It is a partial, single-shot analogue of Phases 2–4 with Phase 1 (mutation) entirely absent — a gap this pipeline design should close. See B.3 for the orchestrator's full comparison.

### A.2 The read-only/CI-write duality — the single most load-bearing mechanism

There are **two independent booleans**, set by different signals, deliberately kept orthogonal:

| Variable     | Question it answers                                            | Set by                                                                                                                            |
| ------------ | -------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- |
| `CI`         | May I prompt interactively / retry in a loop?                  | Explicit `CI=true`, **or** `CLAUDECODE=1` ("Claude Code environment detected — enabling CI mode"), **or** no TTY on stdin/stdout. |
| `qaReadOnly` | May the mutating tools (Rector, PHP CS Fixer) **write files**? | `QA_READONLY` env override, else `GITHUB_ACTIONS=true` → `true`, else `false`. **Claude Code / non-TTY is not in this list.**     |

The header comment in `functions.inc.bash` states the intent explicitly: these were *historically conflated* under `CI`, which made it impossible to both (a) run a real verification gate that fails-instead-of-applies, and (b) let a non-interactive Claude/local session actually apply fixes. Splitting them fixes both. Concretely, three regimes fall out:

| Context                   | `CI`  | `qaReadOnly` | Behaviour                                                                                                                            |
| ------------------------- | ----- | ------------ | ------------------------------------------------------------------------------------------------------------------------------------ |
| GitHub Actions            | true  | true         | Single dry-run pass; a pending diff **fails the gate** with remediation text; no retry (retrying a dry-run can't change the result). |
| Claude Code session       | true  | false        | **Writable** — applies changes directly. `CI=true` only suppresses the interactive retry prompt.                                     |
| Real interactive terminal | false | false        | Writable **and** interactive — prompts `(y/n)` to retry on failure.                                                                  |

**Rector's implementation**: `qaReadOnly=true` appends `--dry-run`, runs once, and interprets exit codes itself (0 = clean, 2 = pending changes → `reportReadOnlyWouldModify`, anything else = genuine error). `qaReadOnly=false` runs in the retry loop until clean.

**PHP CS Fixer's implementation** adds a third, always-fatal category that exit codes alone don't capture: it `grep`s its own text output for `"Files that were not fixed due to errors"` (a lint/parse failure) and exits 1 regardless of read-only/writable mode or numeric exit code — the exit-code taxonomy alone was insufficient.

**Caution found in the same codebase**: not every mutating tool adopted this pattern — `phpStrictTypes.inc.bash` unconditionally does `read -p "Would you like to fix?"` with no `CI`/`qaReadOnly` gating at all, a latent hang risk in any non-TTY context. `ts-qa-ci` should apply the gate **uniformly to every mutating tool from the start**, not retrofit it tool-by-tool the way `php-qa-ci` evidently did.

**A shared remediation-message helper** (`reportReadOnlyWouldModify`) prints a fixed template naming the human tool, the exact local command to run with `QA_READONLY=0`, and the git add/commit follow-up — worth porting as a single shared helper, parameterised per tool.

**Mapping to Prettier/ESLint**:

- **`CI` vs `qaReadOnly` split itself** — TRANSFERS-DIRECTLY. The env-var signals (`GITHUB_ACTIONS`, `CLAUDECODE`, TTY detection) are platform-namespaced, not language-namespaced; `ts-qa` should implement its own `detectReadOnly()`-equivalent as one reusable function.
- **Prettier `--check`/`--write`** — TRANSFERS-DIRECTLY at the concept level, but NEEDS-TS-ADAPTATION on exit-code disambiguation: Prettier does not cleanly separate "clean" / "pending diff" / "genuine parse error" by exit code the way PHP CS Fixer's verified codes (0/8) do. `ts-qa` needs to empirically verify (in the spirit of `php-qa-ci`'s "(verified)" comments) which Prettier versions give which codes/output shapes and encode that as a tested contract, likely with the same stdout-grep fallback PHP CS Fixer uses.
- **ESLint `--fix`/no-`--fix`/`--fix-dry-run`** — NEEDS-TS-ADAPTATION, and structurally harder than Prettier: ESLint mixes auto-fixable and report-only rules *within one invocation*, whereas `php-qa-ci` keeps mutating tools (Rector, CS-Fixer) and pure-validation tools (PHPStan, PHPArkitect) strictly separate by phase. `ts-qa-ci` must decide explicitly whether ESLint runs twice (fix-capable under the read-only gate, then report-only for whatever `--fix` couldn't resolve) or once with mode selected by `qaReadOnly`, using the same crash/failure/clean triage (0 = clean, 1 = problems found, 2 = fatal config/crash — never retry).
- **Prettier/ESLint rule-ownership overlap** — a new consideration with no PHP-side precedent: Rector and CS-Fixer never fight over the same lines; Prettier and ESLint can, unless `eslint-config-prettier` (or the flat-config equivalent) is wired in. `ts-qa`'s Phase 1 rule-jurisdiction split must be designed fresh.
- **`tsc --noEmit`** — TRANSFERS-DIRECTLY, and the duality DOESNT-APPLY to it: like PHPStan/PHPArkitect it is inherently non-mutating, so it belongs unconditionally outside the `qaReadOnly` branch.

**Confirmed gap in this repo today**: `/workspace/package.json` has `format`/`format:check` (the write/read-only pair for Prettier exists) but **no `--fix` script for ESLint at all**, and `/workspace/.github/workflows/ci.yml` runs `npm ci` → `npm run build` → deploy only — it invokes neither `format:check`, `lint`, nor `type-check`. This directly contradicts `CLAUDE.md`'s "Deployment Process" section (which claims Prettier auto-formats and commits, and TypeScript/ESLint block deployment on failure). There is no husky/lint-staged/git-hook wiring either. **The mutate/verify duality has zero enforcement in this repo's CI today** — it is pure aspiration in the docs, and `php-qa-ci` is the only working precedent to port from; ec-site provides no working example of this specific mechanism either (see A.8/B.3).

### A.3 Tool delivery and dependency encapsulation

`php-qa-ci` solves a problem specific to **Composer's flat, single-version dependency graph**: every consuming project's tool version must resolve to one shared version tree-wide. Four mechanisms address this:

1. **PHARs via PHIVE** (PHPStan, PHP CS Fixer, Infection, Composer Require Checker, PHPArkitect) — self-executing, GPG-verified, committed binaries in `vendor-phar/`, entirely bypassing Composer's resolver.
2. **Isolated Rector sub-project** (`tools/rector/composer.json`) — Rector itself depends on `phpstan/phpstan` internally; giving it its own composer.json/vendor/ prevents that transitive dependency leaking into the consumer's tree.
3. **The `replace: {"phpstan/phpstan": "*"}` trick** — tells Composer's resolver "this package satisfies any `phpstan/phpstan` constraint, don't actually install it," so PHPStan *extension* packages (which declare `require: phpstan/phpstan`) install cleanly without pulling in a conflicting real copy alongside the PHAR.
4. **`PhpStanGuardPlugin`** — a Composer plugin doing post-install runtime verification: errors if the consumer directly required `phpstan/phpstan` anyway (defeating the `replace` trick), and cross-checks the bundled PHAR's version against what installed extensions expect via `Semver::satisfies()`.

**None of this transfers as a mechanism** — npm's **nested** dependency model has no single flat `vendor/` where every package must agree on one tool version, so the problem these four mechanisms solve is structurally absent. All four are **DOESNT-APPLY**.

**What does transfer is a narrower, genuinely open npm-native question**: peerDependencies vs bundled dependencies. Tools that must be the *exact same instance* the consumer's own code is compiled/linted against (because they participate in the consumer's own module/type graph) should be `peerDependencies`: `typescript` (a second nested TS compiler instance produces confusing, version-mismatched diagnostics), `eslint` (custom rule/plugin APIs are version-specific; a nested ESLint would fight the consumer's own binary/config resolution), and `vite` if `ts-qa-ci` ships Vite plugins. Tools that run as independent, self-contained subprocess checks with no runtime coupling (markdown-link checker, standalone formatter) can safely be ordinary `dependencies` — npm's nesting means no consumer conflict is possible. This is **NEEDS-TS-ADAPTATION**: same underlying goal as the PHAR/replace/isolation cluster, achieved via npm's native `peerDependencies` mechanism instead.

`PhpStanGuardPlugin`'s *diagnostic intent* (detect and warn on consumer-conflict) is worth keeping but not its mechanism: npm's own `peerDependencies` warning (native since npm 7) already covers most of this need, so `ts-qa-ci` mainly needs to pick the right dependency type rather than build a custom guard plugin. NEEDS-TS-ADAPTATION.

**ESLint plugin/rule delivery is already solved, natively, in TS** — and ec-site is a working example: under ESLint's flat-config system, a "plugin" is just a plain JS object with a `rules` map. ec-site's `eslint.config.js` imports local rule modules directly (`import noPlaceholder from './eslint-rules/no-placeholder.js'`) and namespaces them under a `custom` plugin object — no `eslint-plugin-*`-named package, no legacy plugin-name resolution. `ts-qa-ci` should ship its rule set the same way: plain exported objects/arrays a consumer imports and spreads into their own `eslint.config.js`. **TRANSFERS-DIRECTLY as a design pattern, and ec-site already implements the TS-native version of it.**

A narrow surviving case for a "pinned binary" concept (the PHIVE goal, not its mechanism): if `ts-qa-ci` wants a genuinely decoupled, reproducible tool version (e.g. a canonical CI-gate `tsc`/`eslint` version distinct from whatever the consumer has), npm's lockfile + registry integrity hashes already provide this natively via ordinary `dependencies` — no bespoke vendoring layer needed. DOESNT-APPLY as "needs a PHIVE-equivalent," the goal is pre-solved by the platform.

### A.4 Configuration cascade and override system

Two independent resolution primitives (not one unified engine), plus a third precedence mechanism:

**`configPath()`** resolves a config *file*: `{project}/qaConfig/{file}` → `configDefaults/{platform}/{file}` → `configDefaults/generic/{file}`. Notable finding: `configDefaults/` on disk today contains **only** `generic/` — the platform tier is architecturally real but currently unpopulated for Symfony (the docs show a hypothetical `configDefaults/symfony/phpstan.neon` that doesn't exist as a file).

**`runTool()`** resolves a tool *script*: `{project}/qaConfig/tools/{tool}.inc.bash` → `includes/{platform}/{tool}.inc.bash` → `includes/generic/{tool}.inc.bash`, exclusive first-match-wins (no merging). A real example of platform behaviour *composing with* (not just replacing) generic behaviour exists: `includes/symfony/setConfig.inc.bash` explicitly `source`s the generic script first, then adds Symfony-only variables — extension is opt-in and explicit, not automatic merging.

**Bash `${var:-default}` precedence** for scalar/array config variables — a 2-step precedence (defaults set first, project config sourced after and unconditionally wins), not a file cascade.

**Doc/code drift found and worth heeding as a caution**: `CLAUDE.md`'s "6-step cascade" narrative references a `configDefaults.inc.bash` file that does not exist anywhere in the repo (it's shorthand for scattered `${var:-default}` assignments), and `docs/platform-detection.md` documents Laravel detection that `detectPlatform()` never implements (only Symfony via `symfony.lock` presence, else generic). `php-qa-ci` itself only ships 2 real platforms despite documenting a third.

**Classification**:

| Concept                                                                                    | Classification                                                                                                                                                                                                                                           |
| ------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Dual resolution primitives (config-file cascade vs. tool-script cascade, as two functions) | TRANSFERS-DIRECTLY                                                                                                                                                                                                                                       |
| 3-tier search order, first-match-wins, no merging                                          | TRANSFERS-DIRECTLY                                                                                                                                                                                                                                       |
| `${var:-default}` env precedence                                                           | NEEDS-TS-ADAPTATION — use an explicit merge (`{...builtinDefaults, ...projectConfig, ...envOverrides}`) with a deliberately chosen precedence order, avoiding the footgun where php-qa-ci's project file unconditionally clobbers a pre-exported env var |
| Platform detection via marker file                                                         | NEEDS-TS-ADAPTATION — check `vite.config.*`/`next.config.*` presence, or `package.json` deps, for Vite/Next.js/generic                                                                                                                                   |
| Composition pattern (platform explicitly re-imports/extends generic)                       | TRANSFERS-DIRECTLY                                                                                                                                                                                                                                       |
| Doc/code drift risk (documented-but-unimplemented platforms)                               | NEEDS-TS-ADAPTATION as a process caution — keep `detectPlatform()`-equivalent code tested against its own docs                                                                                                                                           |
| Log rotation/archival per tool; single lock file per project                               | TRANSFERS-DIRECTLY                                                                                                                                                                                                                                       |

Neither this repo nor ec-site currently implements anything like this cascade — ec-site's QA tooling (`README-QA.md`, `Dockerfile.ts-qa`) is a narrowly-scoped, per-code-snippet Docker harness with a hardcoded single config and no override points; this repo's `llm:qa` chain is a fixed, bespoke sequence. This is genuinely new work for `ts-qa-ci`, modelled on `php-qa-ci`'s cascade with npm-native mechanics (filesystem `existsSync` probes + dynamic `import()`).

### A.5 Hook system and Claude Code integration

**`hookPre.bash`/`hookPost.bash`**: pre-hook runs after preflight config resolution but before any tool executes; post-hook runs only after every phase succeeds (unreached on any upstream failure, since the pipeline is fail-fast). Both are `source`d into the pipeline's own shell, inheriting all pipeline variables. NEEDS-TS-ADAPTATION: the *lifecycle* transfers directly, but bash's `source`-into-parent-shell trick has no JS equivalent — `ts-qa` should dynamically `import()` a project-supplied `tsQaConfig/hookPre.ts`/`hookPost.ts` module exporting a callback the orchestrator invokes with a typed context object (safer than blind `source` regardless). Neither this repo nor ec-site has an equivalent project-level lifecycle-hook extension point today.

**Per-tool override files** (`runTool()`'s cascade, A.4) let a project *completely replace* a tool step, not just tweak parameters. TRANSFERS-DIRECTLY as policy (project override > platform default > generic default), NEEDS-TS-ADAPTATION as mechanism (dynamic import of a resolved `.ts` module instead of `source`).

**`deploy-skills.bash`** (705 lines) is what pushes skills/agents/hooks into a consumer project on `composer install`/`update`, via a Composer plugin (`SkillsDeployPlugin`) subscribed to `POST_INSTALL_CMD`/`POST_UPDATE_CMD`. It:

- Copies `.claude/skills/*`, `.claude/agents/*.md`, `.claude/hooks/*.py` (with clobber protection — full directory replace, then `chmod +x`).
- Installs a git pre-commit hook only if absent or already carrying its own signature comment (never clobbers a hand-authored hook).
- Idempotently merges hook registrations into `settings.json` (byte-diff before write, no gratuitous rewrites), with a rename-migration dict for renamed hook files across versions.
- **Critically**: when a hooks-daemon is detected, it actively **removes** all classic `php-qa-ci__*.py` hook files and strips their `settings.json` entries, on the theory the daemon now provides that functionality — a classic-to-daemon migration precedent directly relevant here, since this repo already runs the hooks-daemon exclusively (confirmed via this session's system prompt `<hooksdaemon>` block).
- The shipped hook catalogue (`php-qa-ci__auto-continue.py`, `php-qa-ci__prevent-destructive-git.py` — both "recommended for all projects" — plus optional ones for git-stash discouragement, plan-time-estimate blocking, doc-content validation, markdown organisation) maps near-1:1 onto this project's own hooks-daemon handlers (`destructive_git`, `git_stash`, `plan_time_estimates`, `validate_instruction_content`, `markdown_organization`) — three independent LTS implementations converging on the same policy set.

**`PHP_QA_CI_DISABLE_CONFIG_PUSH`** opt-out: read first thing in `deploySkills()`, accepts truthy strings (`true`/`1`/`yes`/`on`), and — notably — the plugin **proactively logs the opt-out instructions on every run**, even when not using it, so deploy operators can discover the flag. The documented rationale: auto-deploy on `composer install`/`update` can leave a dirty working tree on hosts where that's unwanted (CI runners, staging/prod deploys that aren't Claude Code sessions).

**Classification and the npm-specific risk this surfaces**: the Composer-plugin-subscribes-to-lifecycle-event pattern is TRANSFERS-DIRECTLY as *precedent for the tradeoff*, NEEDS-TS-ADAPTATION as mechanism — npm's nearest primitive is `package.json`'s `"postinstall"` script, which is materially weaker than Composer's plugin model: `npm install --ignore-scripts` (or a persisted `.npmrc` setting) silently skips it with **no equivalent logged feedback**, and `pnpm` blocks *all* dependency lifecycle scripts by default since v7 as a supply-chain mitigation. Given a growing corporate/CI trend toward blanket `--ignore-scripts` policies, an npm `postinstall` auto-deploy has a materially weaker guarantee than `php-qa-ci`'s Composer-plugin equivalent, and any opt-out env var it self-checks is bypassed entirely by `--ignore-scripts`/pnpm defaults before the deploy script ever runs. This is decision-relevant for Task 2.5 — see Part C.

### A.6 Rule-tiering philosophy

Two lessons from `php-qa-ci`'s PHPArkitect/PHPStan split, both directly relevant to designing the CDD ESLint tier.

**1. "Where does a rule belong" decision guide.** README states it plainly: *"Default to PHPArkitect for structural rules. Upgrade to a PHPStan rule only when you need finer-grained, method-level, or semantic detection that arkitect cannot express."* PHPArkitect reasons about a class's *identity* (kind, name, namespace, ancestry) — naming conventions, namespace layering, dependency direction. PHPStan reasons about *code* — method-level predicates, composite name-pattern logic, behavioural/semantic checks. **SSoT principle**: never enforce one convention in both engines — when a structural rule already exists in the semantic engine, *migrate* it to the structural engine and delete the duplicate, rather than running both (concrete precedent: the Interface/Enum/Trait suffix convention was migrated from a PHPStan rule to arkitect).

ESLint has no two-engine split (a single AST pass can express both identity and behavioural checks), so the *engine* distinction doesn't transfer — but the underlying decision axis does: identity/structural predicate vs semantic/behavioural predicate is still the right question when deciding which ESLint rule *tier* a check belongs in. The SSoT "never duplicate across two enforcement points" principle transfers directly and is the more important half.

**2. Always-on vs opt-in tiers, and the "estate-wide checks can't be opt-in" lesson.** `php-qa-ci` ships PHPStan rules in three tiers: always-on (`rules-default.neon`, wired automatically), opt-in generic (`rules-optional.neon`), opt-in Symfony (`rules-optional-symfony.neon`, consumer adds an `includes:` line). PHPArkitect has the identical 3-tier shape. But the **SensitiveParameter usage check is deliberately NOT a PHPStan rule at all** — it's a separate, always-on *pipeline tool* invoked unconditionally inside `bin/qa`, with the reasoning stated twice for emphasis: *"PHPStan rules are opt-in (a consumer must include this library's rules neon), so they cannot be relied on estate-wide."* The escape hatch is opt-**out** (`useSensitiveParameterCheck=0`), not opt-in — an asymmetry that flips the estate-wide coverage guarantee: opt-out (default-on, rare escape) vs opt-in (default-off, rare adoption) produce opposite guarantees for the same rule.

**Classification**: TRANSFERS-DIRECTLY as design patterns (tiered rule sets with opt-in escalation; the SSoT principle; the opt-in-can't-be-estate-wide lesson), NEEDS-TS-ADAPTATION for mechanics — ESLint flat config has a native, arguably better-suited idiom than PHPStan's `.neon` includes: exported config arrays (`rulesDefault`, `rulesOptional`, `rulesOptionalCdd`) a consumer spreads into their own `eslint.config.js`. Neither this repo nor ec-site currently implements a tiered always-on/opt-in split for multiple consumers — ec-site's 57 rules are all registered flat in one config because ec-site is the sole consumer of its own rules; it has never had to solve the multi-consumer tiering problem. This is genuinely new work, directly informing Part C's CDD-tier recommendation.

### A.7 Documentation structure and style

`php-qa-ci` uses a two-tier docs system: a top-level `README.md` with a docs index linking out, a `docs/` folder of focused single-topic files, and a `docs/tools/` subfolder for one-file-per-tool deep dives. Observed conventions worth adopting verbatim: short declarative headers with no fluff intro; numbered pipeline phases with bold tool name + one-line purpose; **relative links to actual source files with line anchors** rather than restated interfaces (directly matching this repo's own `CLAUDE.md` documentation-standards rule: *"NEVER replicate actual interfaces in documentation… link to the source file"*); config cascade explained once, precisely, with a concrete worked example rather than an abstract description; every tool doc follows a fixed shape (what it is → default config location → override mechanism → bundled extensions/rules table → troubleshooting with runnable snippets); every "how to enable/opt out" instruction pairs a one-line rationale with a copy-pasteable snippet. `CLAUDE.md` itself is a separate, longer, implementation-detail-oriented doc distinct from `docs/` — internals for someone modifying the tool, vs consumer-facing usage docs.

**Classification**: TRANSFERS-DIRECTLY — this structure is language-agnostic and directly informs Task 3.5 (docs set: `docs/pipeline.md`, `docs/configuration.md`, `docs/tools/*.md`, `docs/coding-standards.md`, `docs/cdd-rules.md`, `docs/github-actions.md`). Neither ec-site nor this repo currently has an equivalent `docs/` tree for QA tooling (only a single flat `README-QA.md`); adopting this shape for `ts-qa-ci` is new work, not a rename.

### A.8 CI/GitHub Actions patterns: the inline-barrier autofix-then-gate pattern

Two jobs in one workflow file, chained with `needs:`: `autofix` (PR-only, writes fixes, commits back) → `gate` (read-only verification, runs on the fixed tip).

**Mechanics**: `autofix` (gated `if: github.event_name == 'pull_request'`) checks out the PR branch tip (`ref: github.head_ref`, `fetch-depth: 0`), runs the orchestrator in explicit write mode (`QA_READONLY=0`), and pushes back with the default `GITHUB_TOKEN` — skipping cleanly if the tree is clean. `gate` (`needs: autofix`, skipped if autofix failed) re-checks out the **same tip autofix just pushed to**, and runs the orchestrator in its normal CI-auto-detected read-only mode, letting mutating tools dry-run-and-fail on any pending diff rather than silently reapplying it.

**The load-bearing insight — why no PAT or re-trigger is needed**: *"The gate validates the fixed tip in the same run. The autofix push uses the default `GITHUB_TOKEN`, which deliberately starts no new workflow run — so there is no loop, no cancellation, and nothing to wait for."* GitHub Actions' anti-recursion rule (a push made with the built-in token never re-triggers `on: push`) would force a PAT and risk an infinite-trigger loop under a naive "commit then wait for fresh CI" design; keeping both steps in one execution graph sidesteps this entirely.

**Safety properties**: autofix write-back only runs on PRs, never on a push to the default branch; `permissions: contents: write` is scoped to only the job that needs it (workflow default is `contents: read`); an explicit env var neutralises unrelated tree-writing side effects (a Composer plugin's own config-push) before the read-only gate runs, so the gate doesn't false-fail on drift it didn't cause; `composer install --no-scripts` avoids framework post-install hooks needing runtime env absent on the runner. Contrasted explicitly against a weaker single-job template that commits fixes *at the end* of one run — meaning the validating tools ran against **unfixed** code, a materially weaker guarantee than fix-then-validate ordering.

**Classification**:

| Concept                                                                      | Classification                                                                                                                                                                                                                                                                                |
| ---------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Two-job `autofix` → `gate` via `needs:`, single run, no PAT                  | TRANSFERS-DIRECTLY                                                                                                                                                                                                                                                                            |
| Dynamic-default-branch guard; concurrency group + cancel-in-progress         | TRANSFERS-DIRECTLY                                                                                                                                                                                                                                                                            |
| Single orchestrator binary with a read/write env flag auto-detecting CI      | NEEDS-TS-ADAPTATION — no such orchestrator exists in this repo yet; must be built                                                                                                                                                                                                             |
| Mutating tools dry-run-and-fail vs silently apply                            | NEEDS-TS-ADAPTATION, but *simpler* in TS: Prettier's `--check`/ESLint's no-`--fix` default mode is already read-only by construction, unlike PHP CS Fixer/Rector which need explicit dry-run flag plumbing                                                                                    |
| Phase-ordered pipeline inside one orchestrator                               | NEEDS-TS-ADAPTATION — genuinely new build, not a rename of existing npm scripts                                                                                                                                                                                                               |
| CI auto-detection (`GITHUB_ACTIONS=true` → force read-only)                  | NEEDS-TS-ADAPTATION                                                                                                                                                                                                                                                                           |
| Neutralising unrelated tree-writing side effects before the gate             | NEEDS-TS-ADAPTATION — principle transfers (e.g. this repo's own `scripts/generate-snippets.mjs` output must be committed or excluded before a gate check)                                                                                                                                     |
| `composer install --no-scripts`; PHIVE/PHAR install; SSH deploy-key bundling | DOESNT-APPLY — no npm equivalent problem class                                                                                                                                                                                                                                                |
| Dynamic PHP-version detection from `composer.json`                           | NEEDS-TS-ADAPTATION — `actions/setup-node@v4` with `node-version-file` from `.nvmrc`/`package.json` does this natively and more simply; worth noting this repo's own `ci.yml` currently **hardcodes** `node-version: '18'` rather than deriving it — an existing gap, not just a porting task |
| Deferring the test phase from the gate (DB/service dependency)               | NEEDS-TS-ADAPTATION, likely doesn't apply — this repo's Vitest suite has no evident DB/service dependency, so the gate could include tests directly; decide explicitly rather than inherit the PHP template's assumption                                                                      |
| Branch-protection-requires-gate-job; coverage/artifact upload                | TRANSFERS-DIRECTLY (coverage: `vitest run --coverage` already exists as an npm script)                                                                                                                                                                                                        |

This repo's current CI (`checkout → setup-node@18(hardcoded) → npm ci → npm run build → deploy`) has **no gate job and no autofix job at all** — the entire inline-barrier pattern is net-new work for Task 4.6, not a migration of an existing but differently-shaped mechanism. ec-site provides no CI-workflow precedent either (its own `llm:qa` scripts are local/manual, not wired into any GitHub Actions job).

---

## Part B: `ec-site` — TS-Native Realisations

ec-site (`edmonds-commerce-site`, confirmed `"private": true` in `package.json`) is a React+TS+Vite+Tailwind LongTermSupport site — the same stack as this repo — that has independently built most of what this plan wants: 57 custom ESLint rules (with paired `.md` docs), working CDD rules including a solved articles/raw-HTML collision, a TS-native orchestrator with agent-oriented caching, and meta-rules that lint the rules. Because ec-site is private, **Part B.5 raises a licensing/provenance flag that is not resolved by this report** and must be signed off before any Phase 3 code lift.

### B.1 Master ESLint rule catalogue — all rules covered across the research batches

All 57 rules live in `/workspace/untracked/ec-site/eslint-rules/*.js`, each paired with a `.md` doc. Three independent research batches (18 rules each) plus the CDD deep-dive (6 rules, 5 of which overlap with the batches and are reconciled below) collectively covered 55 distinct named rules from the full 57. Verdicts below merge all batches; where the CDD deep-dive additionally examined a rule already in a batch table, both angles are folded into one row (noted).

**Verdict key**: **lift-as-is** = usable near-verbatim; **adapt** = real portable concept, needs de-hardcoding of ec-site vocabulary/paths/business logic (or, in one case, safety hardening) before it's package-worthy; **drop** = ec-site business/content/brand logic with no reusable core.

| #   | Rule file                                   | Purpose                                                                                                                                           | Verdict                                        | Classification                                                  | Key reasoning                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| --- | ------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------- | --------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `enforce-contact-link-whitelist.js`         | Restricts `ContactLink`/`tel:`/`mailto:` to Contact page + Footer                                                                                 | drop                                           | DOESNT-APPLY                                                    | Hardcoded ec-site page-path whitelist; brand/content policy, not code quality                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 2   | `enforce-dynamic-data-sources.js`           | Bans hardcoded case-study counts/tech versions; forces sourcing from registries                                                                   | drop                                           | DOESNT-APPLY                                                    | Tied to ec-site's anti-fabricated-metrics content initiative; no generic project has a `CASE_STUDIES` registry to point at                                                                                                                                                                                                                                                                                                                                                                           |
| 3   | `enforce-external-link-whitelist.js`        | Restricts external links to research/technology pages + Footer                                                                                    | drop                                           | DOESNT-APPLY                                                    | Same shape as #1; also **duplicates policy with #13** — a rule-sprawl anti-pattern to flag, not replicate                                                                                                                                                                                                                                                                                                                                                                                            |
| 4   | `enforce-tel-links.js`                      | Enforces E.164 `tel:` formatting                                                                                                                  | adapt (opt-in a11y tier)                       | NEEDS-TS-ADAPTATION                                             | Real UX concern, but UK-specific defaults and most TS/React projects have zero `tel:` links — opt-in "marketing site" tier only                                                                                                                                                                                                                                                                                                                                                                      |
| 5   | `enforce-width-standards.js`                | Bans non-standard Tailwind max-width classes vs. two approved tokens                                                                              | adapt (CDD pattern template)                   | NEEDS-TS-ADAPTATION                                             | Design tokens/exceptions are ec-site's own; the *shape* (narrow single-token banned/allowed list + hardcoded per-file exception table) is Plan 011's own cited `no-ad-hoc-classnames` reference point. See B.2                                                                                                                                                                                                                                                                                       |
| 6   | `no-aside-after-expandable-details.js`      | Flags `<Aside>` as sibling (not child) of `<ExpandableDetails>`                                                                                   | drop                                           | DOESNT-APPLY                                                    | Two named ec-site components' CSS float/flow-root interaction; no generic content                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 7   | `no-browser-globals-in-render.js`           | Bans `window`/`document`/`localStorage` access outside safe render contexts (SSR safety)                                                          | lift-as-is / adapt                             | TRANSFERS-DIRECTLY                                              | Zero business coupling; this repo is **also** SSG (Vite SSR + prerender) so the exact failure mode applies here too. Superseded/complemented by `ssr-safe-hooks.js` (#7 in batch 3, below) which covers a broader set of hydration-mismatch patterns — recommend adapting the union of both rather than either alone                                                                                                                                                                                 |
| 8   | `no-children-on-prop-only-components.js`    | Flags JSX children on components with `children?: never`                                                                                          | adapt                                          | NEEDS-TS-ADAPTATION                                             | **Doc/implementation drift found**: the `.md` claims automatic TS-type-checker detection, but the shipped `.js` uses a hardcoded fallback component list, not the type checker. Build the real type-aware version (via `@typescript-eslint` parser services), don't port the hardcoded list                                                                                                                                                                                                          |
| 9   | `no-duplicate-section-ids.js`               | Flags duplicate literal `id="..."` JSX attributes in one file                                                                                     | lift-as-is                                     | TRANSFERS-DIRECTLY                                              | Pure AST bookkeeping, zero business content, applies to any SSG/prerendered React site. Note: this is a **core-tier** candidate, not CDD-tier — see B.2                                                                                                                                                                                                                                                                                                                                              |
| 10  | `no-empty-highlight-text.js`                | Flags empty `highlightText` prop on two named components                                                                                          | drop                                           | DOESNT-APPLY                                                    | Named ec-site brand hero components                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 11  | `no-eslint-disable.js`                      | Bans all `eslint-disable*`/`@ts-ignore` suppression comments                                                                                      | lift-as-is                                     | TRANSFERS-DIRECTLY                                              | Fully generic policy. **Three independent implementations found** of "don't suppress, fix": this rule, php-qa-ci's `ForbidInlinePhpstanIgnoreRule`, and this project's own hooks-daemon `error_hiding_blocker` handler. Strong always-on core-tier candidate per the estate-wide lesson (A.6)                                                                                                                                                                                                        |
| 12  | `no-expandable-details-in-p.js`             | Flags a block-level component nested inside `<p>` (HTML5 violation)                                                                               | adapt                                          | NEEDS-TS-ADAPTATION                                             | The AST ancestor-walk technique is reusable; the target component name is hardcoded — genericise to a config-supplied list of "known block-rendering component names"                                                                                                                                                                                                                                                                                                                                |
| 13  | `no-external-links-in-pages.js`             | Business policy: marketing pages can't link externally without a registry entry                                                                   | drop                                           | DOESNT-APPLY                                                    | Duplicates #3's policy (rule-sprawl flag); large embedded ec-site technology allowlist                                                                                                                                                                                                                                                                                                                                                                                                               |
| 14  | `no-hard-coded-component-data.js`           | Heuristic: flags hardcoded content arrays in components accepting no data props                                                                   | adapt                                          | TRANSFERS-DIRECTLY (as pattern) / NEEDS-TS-ADAPTATION (as code) | The most sophisticated rule examined — a content-keyword-vs-config-keyword heuristic classifier with no business vocabulary. Best existing ec-site precedent for "typed configuration surface, not ad hoc content," directly informing variant-prop enforcement design. Ships `recommended: false` even in ec-site (inherently fuzzy heuristics) — keep opt-in/warn-tier                                                                                                                             |
| 15  | `no-hardcoded-contact-details.js`           | Regex-detects UK phone/email; requires centralised `siteConfig` import                                                                            | adapt                                          | NEEDS-TS-ADAPTATION                                             | "Centralise PII, don't scatter it" is portable; UK-specific regexes and a hardcoded ICO-number allowlist are not                                                                                                                                                                                                                                                                                                                                                                                     |
| 16  | `no-hardcoded-routes.js`                    | Flags string route literals in favour of a typed `ROUTES` object                                                                                  | adapt (strong candidate)                       | NEEDS-TS-ADAPTATION                                             | Directly matches this repo's own `src/routes.ts` convention. Blocker: `suggestRoutePath`'s fix map is a hardcoded dictionary of ec-site's own routes — adapt using ec-site's own documented **dynamic-load pattern** (`load-routes.js`: `execSync` to load the consumer's real `ROUTES` export at lint time) instead of a static map                                                                                                                                                                 |
| 17  | `no-html-in-pages.js`                       | Bans raw HTML in page files outside a two-tier prose-context whitelist                                                                            | adapt (flagship CDD rule)                      | NEEDS-TS-ADAPTATION                                             | Named explicitly in `PLAN.md` as the direct ancestor of `no-ad-hoc-html`. Full mechanism deep-dive in B.2                                                                                                                                                                                                                                                                                                                                                                                            |
| 18  | `no-json-content-type-for-apps-script.js`   | Auto-fixes `Content-Type` to dodge a Google Apps Script CORS quirk                                                                                | drop                                           | DOESNT-APPLY                                                    | Single-purpose vendor-integration workaround, not a QA rule                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 19  | `no-non-deterministic-render.js`            | Flags `new Date()`/`Math.random()` in render (SSR/SSG hydration mismatch)                                                                         | adapt                                          | NEEDS-TS-ADAPTATION                                             | 100% framework-generic AST logic; belongs in an opt-in "React SSR/SSG" tier since not every project SSRs. Overlaps with `ssr-safe-hooks.js` (#7 batch 3) which covers this exact case plus more                                                                                                                                                                                                                                                                                                      |
| 20  | `no-orphaned-grid-items.js`                 | Ensures grid item counts are multiples of column count                                                                                            | drop                                           | DOESNT-APPLY                                                    | Content-completeness QA (design-shape validation), not HTML/CSS purity — hardcoded component names and exemption regexes. Its agent-suggests/human-authorises suppression-comment governance language is reusable text regardless. See B.2                                                                                                                                                                                                                                                           |
| 21  | `no-page-component-data-exports.js`         | Bans named exports from page files (preserves Vite code-splitting)                                                                                | adapt                                          | NEEDS-TS-ADAPTATION                                             | Real bug class for lazy-loaded Vite SPAs; **moot for this repo specifically**, which SSG-prerenders rather than lazy-loads pages                                                                                                                                                                                                                                                                                                                                                                     |
| 22  | `no-placeholder.js`                         | Bans the literal string `"PLACEHOLDER"` anywhere in string/template literals                                                                      | lift-as-is                                     | TRANSFERS-DIRECTLY                                              | Fully generic, zero coupling. Directly relevant to this repo's own `articles.ts` content pipeline (no equivalent check exists here today)                                                                                                                                                                                                                                                                                                                                                            |
| 23  | `no-raw-block-html-in-articles.js`          | Bans raw block HTML (`pre`/`table`/`div`) in article JSX, allows inline prose                                                                     | drop (concept independently validated)         | DOESNT-APPLY (mechanism)                                        | This is the plan's own cited "solved articles-collision" precedent. **Cannot transplant as-is**: depends on articles being JSX; this repo's articles are template-literal strings in `articles.ts`, invisible to any JSX AST rule. See B.2 for the governing principle that *does* transfer                                                                                                                                                                                                          |
| 24  | `no-route-comments.js`                      | Forbids `/* Route: */`-style stale structural comments in pages                                                                                   | drop                                           | DOESNT-APPLY                                                    | Targets one team's specific comment-authoring habit; too vague to encode without a concrete recurring pattern elsewhere                                                                                                                                                                                                                                                                                                                                                                              |
| 25  | `no-router-link-hash.js`                    | Flags React Router `<Link to="#...">`, requires native `<a href="#...">`                                                                          | adapt                                          | NEEDS-TS-ADAPTATION                                             | Pure react-router-dom correctness rule (this repo uses React Router v7); belongs in a framework-detected opt-in tier, not unconditional core                                                                                                                                                                                                                                                                                                                                                         |
| 26  | `no-stacked-expandables.js`                 | Bans two expandable components appearing back-to-back with no prose between                                                                       | drop                                           | DOESNT-APPLY                                                    | Entirely ec-site component-catalogue/content-UX logic                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 27  | `no-string-link-props.js`                   | Flags `link`/`to`/`href: string` props, requires typed route objects                                                                              | adapt                                          | NEEDS-TS-ADAPTATION                                             | Portable architectural principle (typed route props over bare strings); hardcodes ec-site's `RouteEntry`/`LinkDestination` type names — needs config-driven type names. Close enough to #28 to merge into one configurable rule rather than ship as two                                                                                                                                                                                                                                              |
| 28  | `no-string-route-properties.js`             | Same principle as #27, one layer down (data/interface properties, not component props)                                                            | adapt                                          | NEEDS-TS-ADAPTATION                                             | Same reasoning as #27; candidate to merge with it                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 29  | `no-unescaped-quotes-in-meta.js`            | Flags single-quoted strings containing literal `"` inside an SEO `seo` export                                                                     | adapt                                          | NEEDS-TS-ADAPTATION                                             | Real, generic bug class (breaks HTML attribute parsing); **directly relevant to this repo**, whose `articles.ts` SEO `description` fields rely on manually-documented escaping rules in `CLAUDE.md` with no automated check backing them. Hardcodes the `seo` export name and property list — needs to be configurable                                                                                                                                                                               |
| 30  | `no-window-location.js`                     | (a) bans `window.location.href=`/`.assign()` for React-Router navigation; (b) requires `rel="noopener noreferrer"` on `target="_blank"` links     | adapt (split)                                  | NEEDS-TS-ADAPTATION                                             | Concern (b) is a framework-agnostic web-security best practice with zero business logic — the stronger core-tier candidate. Concern (a) is React-Router-specific and belongs in the same opt-in tier as #25. Splitting the two concerns is the adaptation                                                                                                                                                                                                                                            |
| 31  | `prefer-satisfies-over-as-const.js`         | Auto-fixes `as const` to `satisfies TypeName` for known property names                                                                            | adapt                                          | NEEDS-TS-ADAPTATION                                             | Real modern-TS idiom; hardcodes ec-site's own property→type lookup table. Caution: `as const` and `satisfies` are not strict substitutes — a naive blind auto-fix risks incorrect fixes where no matching external type exists. Needs config-driven property→type mapping, not a blind global fix                                                                                                                                                                                                    |
| 32  | `require-component-default-export.js`       | Requires a default export alongside named PascalCase component exports (for `React.lazy()`)                                                       | adapt                                          | NEEDS-TS-ADAPTATION                                             | Real Vite/React code-splitting correctness check; **moot for this repo** (SSG-prerenders, doesn't lazy-load pages), same as #21                                                                                                                                                                                                                                                                                                                                                                      |
| 33  | `require-component-in-design-guidelines.js` | Fails unless every component is referenced in one hardcoded design-guidelines page                                                                | drop                                           | DOESNT-APPLY                                                    | Fully coupled to ec-site's single hand-maintained page and filesystem conventions                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 34  | `require-documented-component-exists.js`    | Fails if a `CLAUDE.md`-documented component path doesn't exist on disk                                                                            | drop (concept independently validated)         | DOESNT-APPLY                                                    | Ec-site-specific markdown-table format/path. **This project's own hooks-daemon `plan_qa_sweep`/`row-folder-bijection` checks independently solve the identical "doc↔file bijection" category** for plan folders — a second, unrelated convergence. If ts-qa-ci ever builds a generic version, it should be config-driven (markdown table format/columns), not ported from either existing instance                                                                                                   |
| 35  | `require-explicit-type-annotations.js`      | Requires `const` data literals to carry an explicit, imported type annotation                                                                     | lift-as-is                                     | TRANSFERS-DIRECTLY                                              | Zero business logic; a pure TS strictness rule. Direct cross-language analogue: php-qa-ci's `RequireDeclareStrictTypesRule` — same "explicit typing over inference" philosophy, validated independently on both sides of the PHP/TS split                                                                                                                                                                                                                                                            |
| 36  | `require-exported-component-types.js`       | Requires `*Props` types under `src/components/` to be exported                                                                                    | lift-as-is                                     | TRANSFERS-DIRECTLY                                              | Generic component-library hygiene; only ec-specific bits are trivially-configurable default path/skip-list. Loose PHP analogue: php-qa-ci's `RequireApiOrInternalTagRule`/`ApiMustNotExposeInternalRule` (deliberate public-API-surface control)                                                                                                                                                                                                                                                     |
| 37  | `require-layout-variant-for-carousels.js`   | Forces a `layout` variant wrapper around two named carousel components                                                                            | drop                                           | DOESNT-APPLY                                                    | Hardcodes ec-site component names and a 3-way variant enum specific to its design system                                                                                                                                                                                                                                                                                                                                                                                                             |
| 38  | `require-llm-npm-commands.js`               | Enforces every QA npm script has a paired `llm:*` machine-readable variant                                                                        | drop the rule; keep the principle              | DOESNT-APPLY (as a rule)                                        | No PHP-side analogue exists (confirmed via grep). `ts-qa` should bake structured/agent-parseable output natively into its **orchestrator**, superseding the need for consumers to hand-maintain paired npm scripts — see B.3                                                                                                                                                                                                                                                                         |
| 39  | `require-page-layout-wrapper.js`            | Every page file must import and wrap content in `PageLayout`                                                                                      | drop                                           | DOESNT-APPLY                                                    | Hardcoded to one literal import path/component name; the generalisable shape (configurable root-layout-wrapper check) isn't actually what's shipped                                                                                                                                                                                                                                                                                                                                                  |
| 40  | `require-page-seo-export.js`                | Every page must have a sibling `-meta.ts` exporting `seo: PageSEO`; forbids inline export                                                         | adapt                                          | NEEDS-TS-ADAPTATION                                             | Filename-convention pattern generalises well to any Vite/React SSG project; file suffix/type name/pages-root need to become config. Pairs with `validate-seo-metadata.js` (#54) as one opt-in "SEO metadata" tier                                                                                                                                                                                                                                                                                    |
| 41  | `require-sitemap-page-sync.js`              | Diffs a bespoke `CLAUDE/Sitemap/` markdown doc against implemented routes                                                                         | drop                                           | DOESNT-APPLY                                                    | Proprietary planning-doc notation, not a general convention                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 42  | `require-tests-in-page-folders.js`          | Playwright spec files must mirror `src/pages/` structure under `tests/pages/`                                                                     | adapt                                          | NEEDS-TS-ADAPTATION                                             | Portable test-location-mirrors-source convention for folder-based routing; `pagesDir`/`testsDir` roots and smoke-test exception need to be configurable. Good fit for `ts-qa-ci`'s testing-phase conventions (Vitest/Playwright is the pre-committed stack)                                                                                                                                                                                                                                          |
| 43  | `ssr-safe-hooks.js`                         | Flags SSR-hydration-unsafe patterns (browser-global reads without `useSyncExternalStore`, non-deterministic render values, raw `useLayoutEffect`) | lift/adapt (strong candidate)                  | TRANSFERS-DIRECTLY                                              | Zero business logic; references React Error #421. This repo is itself SSG (Vite SSR + prerender), making this one of the best lift candidates in the whole catalogue. Only the isomorphic-hook import path needs to be configurable. Broader/superset of #7 and #19 — adapt the union                                                                                                                                                                                                                |
| 44  | `use-types-not-strings.js`                  | Type-checker-based rule flagging plain-string property values where a union/enum type is expected                                                 | adapt (mechanism, not payload)                 | NEEDS-TS-ADAPTATION                                             | The *mechanism* (real type-aware AST linting via `ESLintUtils.getParserServices`) fills a genuine TS-specific gap with **no PHP-side equivalent** (PHPStan gets this for free from PHP's own type declarations; TS's structural typing lets an untyped string slip through silently). Strip the 176-entry business-vocabulary whitelist, ship the type-checking core as a generic, config-driven `no-stringly-typed-props` rule                                                                      |
| 45  | `validate-case-studies-index.js`            | Ensures a case-studies index page lists every registry ID, both directions                                                                        | drop                                           | DOESNT-APPLY                                                    | Pure ec-site content-model logic; part of a **"registry-listing-sync" pattern cluster** with #47 and #48 — see B.2/Part C for the generalised-rule-generator recommendation instead of porting any one instance                                                                                                                                                                                                                                                                                      |
| 46  | `validate-hash-links.js`                    | Cross-checks hash-link objects against JSX `id="..."` attributes in the same file                                                                 | adapt (strong candidate)                       | NEEDS-TS-ADAPTATION                                             | Almost no business logic; only the link-object property name (`path`) is ec-specific. Honest scope limits documented (same-file only) — worth carrying that documentation discipline over                                                                                                                                                                                                                                                                                                            |
| 47  | `validate-hover-text.js`                    | Validates a `hoverText` prop is a well-formed, ≥100-char, ≥3-sentence paragraph                                                                   | drop                                           | DOESNT-APPLY                                                    | Editorial/copywriting policy encoded as a lint rule — out of php-qa-ci-style scope (code correctness/style/architecture only, no prose-content rules)                                                                                                                                                                                                                                                                                                                                                |
| 48  | `validate-internal-links.js`                | Validates internal links/paths against the real `ROUTES` table (loaded via `execSync`)                                                            | adapt (strong candidate)                       | NEEDS-TS-ADAPTATION                                             | Broadly generic and valuable; needs the routes-file path/export name made configurable rather than hardcoded to `src/routes.ts`                                                                                                                                                                                                                                                                                                                                                                      |
| 49  | `validate-lazy-imports.js`                  | Validates `React.lazy(() => import(...))` paths resolve to real files with default exports, with Levenshtein typo suggestions                     | lift/adapt (strongest candidate in this batch) | TRANSFERS-DIRECTLY                                              | Completely generic; fills a real TypeScript blind spot (TS does not validate dynamic `import()` paths or verify default exports at the import site) — directly analogous to why php-qa-ci layers PHPStan on top of PHP's own gaps. Only `@/` alias resolution needs to match the consumer's real `tsconfig` paths                                                                                                                                                                                    |
| 50  | `validate-research-index-completeness.js`   | Ensures a research-category index lists every child route                                                                                         | drop                                           | DOESNT-APPLY                                                    | Pure ec-site content-model logic; same registry-listing-sync cluster as #45/#48                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 51  | `validate-routes-have-pages.js`             | Cross-validates `ROUTES` ↔ `src/pages/` ↔ `App.tsx` in every direction; most comprehensive rule examined                                          | adapt, but harden first                        | NEEDS-TS-ADAPTATION                                             | Highest-value generic capability in the whole catalogue — structurally close to a PHPArkitect-style "every declared X has an implemented Y" structural check. **Not safe to ship as-is**: uses `eval()` on extracted source text to parse the `ROUTES` object literal, a real code-execution risk in a package that runs over arbitrary consumer source, plus regex (not AST) parsing of `App.tsx`. Must be rebuilt with AST-based extraction (or a safe build-time JSON dump) before any adaptation |
| 52  | `validate-seo-metadata.js`                  | Enforces SEO field length/count constraints on the `seo` object                                                                                   | adapt                                          | NEEDS-TS-ADAPTATION                                             | Thresholds mirror universal Google truncation limits (reasonable defaults), but the rule's own doc admits it duplicates limits also defined in a runtime validator (`validatePageSEO()`) — a duplicated-source-of-truth anti-pattern. Should derive limits from one place (e.g. the same `execSync`-load-real-module pattern several sibling rules already use) rather than hardcode a second copy                                                                                                   |
| 53  | `validate-simple-icons.js`                  | Validates named imports from `simple-icons` exist in that package's real exports                                                                  | drop the instance; keep the pattern            | DOESNT-APPLY (narrow)                                           | Hardcoded to one specific npm package most consumers won't use; the *mechanism* (load a package at lint time, validate named imports against real runtime exports) is a reusable rule template worth remembering, not worth building generically for v1                                                                                                                                                                                                                                              |
| 54  | `validate-tech-logos.js`                    | Validates a bespoke icon config against ec-site's own icon registry and checks referenced SVGs exist                                              | drop                                           | DOESNT-APPLY                                                    | Entirely ec-site data-model specific                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |

**Summary counts**: **lift-as-is / TRANSFERS-DIRECTLY**: 8 rules (#7, #9, #11, #22, #35, #36, #43, #49). **adapt / NEEDS-TS-ADAPTATION**: 25 rules. **drop / DOESNT-APPLY**: 21 rules (several with an independently-validated *category*, noted per-row).

**Cross-cutting lessons for Phase 2/3, not captured by any single row**:

- **Rule-sprawl anti-pattern**: `enforce-external-link-whitelist.js` (#3) and `no-external-links-in-pages.js` (#13) enforce near-identical policy via two separately-maintained rules. Flag to the Task 1.2 Opus review as a duplication pattern to avoid replicating, distinct from the individual verdicts.
- **"Registry-listing-sync" is a recurring shape, not three unrelated rules**: #45, #48 (partially), #50 all implement "does everything in source-of-truth X appear in listing/consumer Y, in both directions" at different scopes. A single configurable "registry-sync" rule generator (if ever needed) is a better investment than porting any one instance — and not needed for this repo's v1 scope, which has no case-studies/research registries.
- **`use-types-not-strings.js` and `no-placeholder.js` are TS-only territory** — grepping php-qa-ci's rule set for `satisfies`/`as const`/placeholder-detection equivalents found none; these fill gaps PHP's own type system or php-qa-ci's rule set never had to address.
- **`validate-seo-metadata.js`'s duplicated-limits anti-pattern** is a "do it this way, not that way" example: several sibling rules (`validate-case-studies-index.js`, `validate-internal-links.js`, `validate-research-index-completeness.js`) already avoid it correctly by loading the real registry/type at lint time via `execSync('npx tsx --eval ...')` rather than hardcoding a second copy.

### B.2 CDD rules deep-dive

**`no-html-in-pages.js` — the flagship CDD rule, and `PLAN.md`'s own cited ancestor of `no-ad-hoc-html`.** A single `JSXElement` visitor, filename-gated to `src/pages/**/*.tsx` with explicit filename-string exemptions (404 page, case-studies/technologies subtrees, articles index). Violation logic is a flat, near-exhaustive banned-HTML-element denylist with two ancestor-walked escape hatches: a **lenient** prose context (one named wrapper component: all HTML allowed inside it) and a **strict** prose context (two named wrappers: only a small inline-prose whitelist allowed, block elements like `pre`/`table`/`div` remain banned even here, forcing component use). The report message embeds fix guidance directly (element→suggested-component mapping) rather than relying solely on the paired `.md` doc. The fix-prompt doc assigns a dedicated component-builder agent and is explicit about the CDD boundary this whole tier depends on: *"No suitable component exists → Flag for manual review (DO NOT create ad-hoc components)."* Config wiring in `eslint.config.js` also demonstrates a deliberate, documented single-purpose override: the rule is turned back off for the one demo/catalogue page whose entire purpose is showing raw component usage.

Adapting this for `ts-qa-ci` requires two real changes, not just config-parameterisation: (1) Plan 011's own scope is wider than ec-site's — `no-ad-hoc-html` must cover both `src/pages/**` **and** `src/components/**` in this repo, where ec-site's rule assumes components are pre-vetted and only polices pages; (2) this repo has **zero** prose-context components (no `ProseSection`/`ArticleContent` equivalents — its articles are raw HTML strings, not JSX), so the lenient/strict escape-hatch branches have no target and must be redesigned around `dangerouslySetInnerHTML` article rendering instead, or simply omitted since Decision 3 already exempts `articles.ts` entirely.

**`no-raw-block-html-in-articles.js` — the precedent for the articles-HTML boundary, but not directly usable.** This rule draws its line not at "prose vs no HTML" but specifically at **inline/prose elements vs block-level structural elements**: `pre`/`table`/`thead`/`tbody`/`tr`/`th`/`td`/`div` are banned (each with a component-replacement suggestion), while `p`/`h2`–`h4`/`strong`/`em`/`code`/`ul`/`ol`/`li`/`blockquote` are left alone as legitimately-styled prose. It only works because ec-site's articles are `.tsx` JSX files an AST rule can see into. **This repo's articles are JavaScript template-literal strings in `articles.ts` (19,714 lines)** — an ESLint AST visitor cannot see inside a `TemplateLiteral` string at all, so the mechanism is **DOESNT-APPLY**, not merely adapt. What transfers is the **governing principle**: articles are a legitimately different, separately-governed HTML surface, and a CDD tier should draw its boundary at the surface *type* (JSX vs string-content) rather than force one AST rule to police both — this is exactly the reasoning Decision 3 already encodes, and this research substantiates rather than revises it. Notably, this repo has **independently arrived at an equivalent-but-string-based solution to the adjacent "code blocks must be externalised" problem** — the snippet system (`code-snippets/<slug>/*.ext` + `{{SNIPPET:path}}` + `scripts/generate-snippets.mjs`) plus the `article-snippet-enforcer` hooks-daemon handler are the string-content-surface analogue of what ec-site's rule does for JSX. Two independent LTS projects converged on "code/structural blocks must not be authored inline," solved by two structurally different mechanisms appropriate to each project's own content architecture.

**`enforce-width-standards.js`** — scoped to `src/components/sections/*.tsx`, a narrow single-design-token banned/allowed classname list (`max-w-6xl`/`max-w-4xl`/etc. banned in favour of `max-w-7xl`/`max-w-5xl`) with a **hardcoded per-file, per-class exception table** baked directly into the rule. Not fixable via `meta.fixable` — suggests a textual fix in the message only, no auto-apply. This is the closest ec-site rule to "ban ad hoc/arbitrary CSS class strings," but it is narrow (one design token) and its approach (hardcoded banned/allowed lists + hardcoded exceptions) does not scale to "all arbitrary Tailwind classes" — for that, `eslint-plugin-tailwindcss`'s `no-custom-classname` is the actual generalised tool (see below). Useful as a **pattern reference** for any future single-property CDD sub-rule, not as the general `no-ad-hoc-classnames` mechanism itself.

**`no-orphaned-grid-items.js`** — not an HTML/CSS-purity rule at all; a content-completeness check (grid item count vs. column count, resolved via manual ESLint-scope array-length lookup). DOESNT-APPLY to the CDD ad-hoc-HTML/CSS tier. Worth keeping only its **suppression-comment governance language** verbatim: *"🤖 LLM Agents: You may SUGGEST an exception… 👤 Human Authorization: Only project maintainers can AUTHORIZE exceptions via suppression comment"* — directly reusable phrasing for any CDD suppression policy `ts-qa-ci` adopts.

**`no-hard-coded-component-data.js`** — the closest existing precedent to variant-prop enforcement, though it doesn't police CSS classes at all. A genuine heuristic classifier (content-keyword vs config-keyword property-name matching, uppercase-const convention detection, data-prop-pattern regex against a component's actual destructured params) that forces "configuration surface = typed props," the same underlying principle the CDD tier wants for CSS classes ("styling surface = typed variant props"). Its fix-prompt before/after example (move constant to `/src/data/`, add a typed prop, update call sites) is structurally identical to the CDD blueprint (move ad hoc classes into a `cva()` variant map, add a `variant` prop, update call sites). Ships `recommended: false` even in ec-site because the heuristics are inherently fuzzy — a caution to keep any adapted version opt-in/warn-tier rather than always-on error.

**`no-duplicate-section-ids.js`** — unrelated to CDD's HTML/CSS ambitions (a DOM-id-uniqueness correctness check); its two-pass `Program:exit` aggregation technique is a reusable authoring pattern regardless of domain, and the rule itself is a core-tier, not CDD-tier, lift candidate (see B.1 row #9).

**`eslint-plugin-tailwindcss`** — declared and registered in `eslint.config.js`, but a critical finding: its three rules (`classnames-order`, `no-custom-classname`, `no-contradicting-classname`) are scoped to **exactly one file in the whole ec-site codebase** — the single design-guidelines demo page. `no-custom-classname` (the rule that could actually enforce "ban ad hoc/arbitrary CSS classes") is proven to work where it runs, but ec-site has **not** deployed it as a general gate across `src/pages/**`/`src/components/**`. No `settings.tailwindcss` config override exists anywhere in the file — default Tailwind-config detection and default `className`/`cn()`/`cva()` callee detection only. **Practical implication**: ec-site's "component-driven, no ad hoc classes" claim is enforced almost entirely through structural rules (`no-html-in-pages`, `no-hard-coded-component-data`) plus human/PR discipline, not through the class-validity checker running broadly. `ts-qa-ci` would need to **broaden** this scope (to `src/components/**/*.tsx` and beyond) to realise the full CDD ambition ec-site itself only partially demonstrates — the plugin and its rules are directly reusable dependencies, proven to work in this exact stack, but the config is not something to "adopt" wholesale. `prettier-plugin-tailwindcss` (className auto-sorting, complementary and already wired into ec-site's Prettier config) is a separate, directly portable tool.

**`class-variance-authority` (CVA) — the variant-prop convention exists as a dependency and a single working example, not a catalogue.** A repo-wide search of ec-site found exactly **one** component using `cva()`: `src/components/ui/button.tsx`, the canonical shadcn/ui pattern (`cva()` defining named `variants` → class strings, `defaultVariants`, `VariantProps<typeof buttonVariants>` typing the component's props, `cn()`/`tailwind-merge` resolving the final className). **No ESLint rule in the entire 57-rule catalogue references or enforces CVA adoption** — there is no `require-cva-variant-props` or equivalent. ec-site's own `src/components/CLAUDE.md` catalogue (61 components) documents only Button as CVA-backed; other "variant" props documented elsewhere are typically plain hand-written conditional className logic, not CVA maps. This directly grounds and slightly sharpens Plan 011's own Decision 4 finding (this repo has 0 CVA usage, 17 components, no catalogue): ec-site's own infrastructure is thinner than the plan's phrasing implies — there is no mature ec-site variant catalogue to port wholesale, only a single validated minimal template (CVA + `tailwind-merge` + `clsx`, all three already ec-site dependencies) plus the confirmation that this dependency trio is viable in this exact stack. Decision 4's choice to budget catalogue-building as real Phase 4 work, rather than assume ec-site hands one over, is correct and substantiated by this research.

### B.3 The orchestrator (`npm run llm:qa`) and caching convention

ec-site's orchestration is split two ways, not one file:

- **`scripts/qa.ts`** (`npm run qa`) — human-facing, parallel `spawn`-based runner with colour-coded pass/fail and per-check timing, over a fixed `checks` array (ESLint, TypeScript, Prettier, Route Metadata, Smoke Tests — two checks, Spell Check and Markdown Lint, are deliberately commented out with linked tracking issues rather than silently dropped, a pattern worth copying literally: don't delete a known-broken check, comment it with a tracking link).
- **`npm run llm:qa`** — agent-facing, defined directly in `package.json` as a **sequential** `&&` chain (`llm-type-check.ts && llm-lint.ts && llm:test && validate-route-metadata --llm && llm-format-check.ts && smoke-test-ssg.ts`), deliberately sequential (not parallel) so an agent gets one failure at a time against cached JSON rather than an interleaved stream.
- **An explicit parity contract**, stated in a comment in `scripts/qa.ts`: *"If you modify a check below, you MUST update its llm:* equivalent script to maintain parity"\* — a real lesson for `ts-qa-ci`: maintain one designed relationship between the human-readable and machine-readable command surfaces, and anchor the parity rule in a comment next to the code that would otherwise drift.
- Individual `llm-*.ts` scripts share a consistent shape: upfront argument validation that **fails loudly** on invalid paths (not silent skip), spawn the underlying tool with `--format json` where available, write full results to a cache file, print a **summary only** to stdout with example `jq` queries, exit with the underlying tool's code. `llm-fix.ts` additionally **refuses to run unless git status is clean** — a mutation-safety gate directly analogous to `php-qa-ci`'s read-only/CI duality (A.2) and worth replicating verbatim in `ts-qa-ci`'s own auto-fix command. `llm:type-check` is explicitly documented as never cached because `tsc` is a whole-project compiler that cannot check individual files in isolation.
- **Caching** lives under `./var/qa/` (per-tool subdirectories), implemented in `scripts/lib/cache.ts`: cache key = SHA-256(first 12 hex) of normalised CLI args + config-file mtime + a **git-status-hash** (`md5(git status --porcelain)`) — any uncommitted working-tree change invalidates the entire cache, a deliberately coarse but cheap invalidation strategy. Filenames are `<hash>.<timestamp>.json` with an LRU cap (`MAX_CACHE_FILES = 100`) and mtime-based secondary validation on top of the git-hash check. The actual point of the cache is bounding what an agent has to read into context (summary + `jq`-queryable file), not just speed.

**Classification**: TRANSFERS-DIRECTLY as a concept (orchestrator phase list, human/agent output-surface split, argument-validation-fails-loudly discipline, git-clean-required mutation gate), NEEDS-TS-ADAPTATION as implementation — but the caching library specifically is close to a **direct lift**: `ts-qa-ci` can reuse the git-status-hash-invalidation + LRU-cap + per-tool-subdirectory + summary/full-JSON-split design near-verbatim, only making `CACHE_DIR` configurable instead of hardcoded. No equivalent `var/qa/`-style cache convention was found documented for `php-qa-ci` in the material reviewed — this is ec-site's own innovation, and it is more elaborate/battle-tested than anything on the PHP side, making it a genuine value-add ts-qa-ci gets "for free" from having two references rather than one. `require-llm-npm-commands.js` (the rule that polices this npm-script-pairing convention — B.1 row #38) should itself be **dropped as a rule**: `ts-qa`'s own orchestrator baking in structured/agent-parseable output natively supersedes needing consumers to hand-maintain paired `llm:*` scripts per tool.

### B.4 Meta-rules and the synchronous-handler gotcha

`eslint-rules/meta-rules/` contains three rules that lint the *rule files themselves* (explicitly excluding their own subdirectory to avoid self-application recursion), registered under a `meta-lint` namespace scoped to `files: ['eslint-rules/*.js']` so they run as part of the normal lint pass:

| Meta-rule                  | Purpose                                                     | Severity |
| -------------------------- | ----------------------------------------------------------- | -------- |
| `no-async-handlers.js`     | Flags `async` ESLint handler functions                      | error    |
| `require-documentation.js` | Ensures every `eslint-rules/<name>.js` has a matching `.md` | error    |
| `validate-message-ids.js`  | Checks `messageIds` are defined and actually used           | warn     |

`eslint-rules/CLAUDE.md` documents the **critical, load-bearing gotcha** these rules exist to catch: ESLint's rule visitor calls AST handlers **synchronously** as it walks the tree and finalises `context.report()` collection **before** any `await` inside an `async` handler resolves. An async handler's `report()` calls fire on a microtask after ESLint has already finished collecting results for that file — the violation is **silently dropped**, not deferred, with no error and no warning. This is far more dangerous than a crash: it looks like "the code is clean" when the check simply never ran. The prescribed fix is to never do async I/O inside a handler — use synchronous `execSync`/`readFileSync`, load external data once with caching, and throw on failure (fail loud, not silent). This is a fact about ESLint's rule-visitor contract, not an ec-site quirk — it applies regardless of whether rules are authored in `.js` or `.ts`.

**Classification**: TRANSFERS-DIRECTLY across the board (meta-rule concept, the async-handler contract fact, and the "fail loud on external data load" discipline) — the meta-rules themselves are the clearest, lowest-risk lift candidates in the entire research pass, though NEEDS-TS-ADAPTATION applies to their literal code if `ts-qa-ci` authors rules in TypeScript rather than plain `.js` (ec-site's own rules, including the meta-rules, are all `.js` despite the project's TS-only mandate for `scripts/` — that mandate does not extend to `eslint-rules/`, and a TS-authored rule set is genuinely new work, not a straight port).

### B.5 Licensing/provenance flag — explicit sign-off required, not resolved here

ec-site (`edmonds-commerce-site`) is confirmed **`"private": true`**. Plan 011 intends `ts-qa-ci` to become a publishable `@longtermsupport`-scoped npm package. Lifting ec-site's rules, meta-rules, cache library, or orchestrator scripts — even adapted — changes their exposure from "private, internal to one client site" to "public, redistributable, potentially carrying ec-site-specific business/brand logic" (several rules in the B.1 table are explicitly ec-site-brand-specific: `no-hardcoded-contact-details.js`, `enforce-contact-link-whitelist.js`, `validate-tech-logos.js`, `validate-case-studies-index.js`, among others already marked **drop**). This matters for licensing, IP, and whether client-identifying content could leak into a public package.

**This report explicitly does not treat this as resolved.** Per Plan 011's own instruction (Task 1.1: "note the licensing/provenance question… flag for Technical Decisions, do not resolve unilaterally"), the recommendation carried into Part C is: before any Phase 3 code lift, obtain the maintainer's explicit sign-off that (1) ec-site's code may be relicensed/republished under the `@longtermsupport` scope, and (2) the rules to be ported are audited first to separate genuinely generic QA infrastructure (cache library, meta-rule concept, orchestrator shape, the TRANSFERS-DIRECTLY/lift-as-is rows in B.1) from ec-site-brand-specific rules that must stay private or be rewritten from scratch.

---

## Part C: Design Implications for `ts-qa-ci` — Concrete Decisions for Phase 2

This section maps every open Phase 2 task (2.1–2.7) to what Parts A and B imply, and is explicit about what remains genuinely undecided.

### Task 2.1 — Confirm baseline stack, resolve open tool choices

- **Structural/architecture tool (arkitect analogue)**: `dependency-cruiser` is the closer fit for cross-module/layer import-boundary rules (identity/structural checks — A.6's "arkitect reasons about identity" framing), while ESLint AST rules (custom, JS/TS-authored) should own in-file JSX/classname CDD concerns and semantic checks. Whichever is chosen, **apply the SSoT principle from day one** (A.6): don't let a boundary rule exist in both `dependency-cruiser` config and a duplicate ESLint import rule.
- **Mutation-testing tier (Stryker)**: mirror Infection's treatment exactly — optional, opt-in tier, not part of the core always-on phases, matching A.6's always-on/opt-in tiering pattern.
- **knip vs depcheck, markdown-link checking**: **not covered by any of the 12 research sections** — this is a genuinely open gap this report cannot close. Markdown-link checking has a direct conceptual analogue in `php-qa-ci`'s `markdownLinks` tool (A.1) and should transfer directly as a *concept* (check all relative markdown links resolve); the specific TS tool choice needs its own research pass before Task 2.1 can close.

### Task 2.2 — Orchestrator CLI design

Base the design directly on the merged A.1/A.2/A.4 mechanics: four-phase cost ladder (mutate → lint/validate → static-analyse → test), meta-tool-as-ordinary-tool composition, `-t <tool>` single-tool bypass, `-p <path>` scoping with a per-tool path-support allowlist, a resolveConfigPath/resolveToolModule dual cascade (project → framework-detected default → generic default) implemented via filesystem `existsSync` probes and dynamic `import()` (not bash `source`), the CI-vs-interactive retry gate plus the orthogonal fail-fast/aggregate axis, and the read-only/CI-write duality (A.2) as a **first-class, built-in mode from day one**, applied uniformly to every mutating tool (Prettier `--write`, ESLint `--fix`) rather than retrofitted tool-by-tool the way `php-qa-ci` did with `phpStrictTypes.inc.bash`. Layer in B.3's agent-facing additions ec-site already validated: a summary-to-stdout/full-JSON-to-cache-file split (lift the `var/qa/` caching library near-verbatim), a documented human/agent parity contract, and argument validation that fails loudly rather than silently skipping.

### Task 2.3 — Dependency-encapsulation strategy

Resolved in principle by A.3: `typescript`, `eslint`, and (if `ts-qa-ci` ships Vite plugins) `vite` as `peerDependencies` — anything that must be the same instance the consumer's own code runs against. Self-contained subprocess tools (markdown-link checker, etc.) as ordinary bundled `dependencies`. ESLint rule delivery via exported flat-config objects (A.3, B.1's lift-as-is rules) — already proven working in this exact stack by ec-site's `eslint.config.js` + `eslint-rules/*.js` pattern; no separate `eslint-plugin-ts-qa-ci` package or legacy plugin-name resolution needed.

### Task 2.4 — Does `ts-qa` own and run its own ESLint config, or ship includable rule sets?

**Recommendation, directly derived from A.6's SensitiveParameter precedent**: the CDD tier's **core** enforcement (the raw-HTML ban, at minimum) should be **always-on and pipeline-owned**, not merely an exported rule set a consumer must remember to spread into their own `eslint.config.js` — exactly the reasoning `php-qa-ci` used to move SensitiveParameter coverage out of an opt-in PHPStan rule into an always-on pipeline tool: *"opt-in rules… cannot be relied on estate-wide."* The escape hatch should be **opt-out** (a scoped path-glob exclusion, or a rare blanket `useCddRules=0`), not opt-in, mirroring `useSensitiveParameterCheck=0`/`useArkitect=0`, with the opt-out instructions **logged every run** the way `PHP_QA_CI_DISABLE_CONFIG_PUSH` is. The more prescriptive, catalogue-dependent variant-shape rules (does *this* component's variant prop exhaustively map to classes) should stay in an **opt-in tier**, the direct analogue of `rules-optional-symfony.neon` — correctly reserved for rules that presuppose a convention (a variant-prop catalogue) a consumer may not have adopted yet.

### Task 2.5 — Claude Code integration deploy mechanism

A.5 surfaces a materially weaker guarantee for npm `postinstall` auto-deploy than `php-qa-ci`'s Composer-plugin equivalent: `--ignore-scripts` silently skips it with no logged feedback, and `pnpm` blocks all lifecycle scripts by default. **Recommendation**: ship a **manual deploy script** (`npx ts-qa-ci deploy-skills`, Node-authored per Non-Goals) as the v1 default, not `postinstall` auto-deploy — the dirty-tree caveat `php-qa-ci`'s own README documents applies with a strictly weaker safety net on the npm side. If auto-deploy is ever added, it needs the identical opt-out-env-var-plus-logged-instructions UX, understood as best-effort given `--ignore-scripts`/pnpm can bypass it entirely regardless. **Compatibility with this repo's existing hooks-daemon** (a concrete, checkable Task 4.8 requirement): since this repo already runs the hooks-daemon exclusively, `ts-qa-ci`'s deploy script should follow `php-qa-ci`'s own classic-to-daemon migration precedent (A.5) — register any shipped hooks as daemon project-handlers, not classic `.claude/hooks/*.py` files, and must comply with this repo's `hook_registration_checker` policy (all hooks in `settings.json`, zero `hooks` entries in `settings.local.json`, every command routed through the daemon wrapper).

### Task 2.6 — Non-publish CI install mechanism

**Not covered by any of the 12 research sections** — this is a genuinely open gap. None of the material examined `npm ci`-compatible git-dependency, GitHub Packages, or committed-tarball mechanics in any depth. The closest available analogy is weak: `php-qa-ci`'s PHIVE/PHAR pinning (A.3) solves a different problem (reproducible tool *binaries*, not a reproducible *package reference* for a not-yet-published dependency) and was already classified DOESNT-APPLY. Flag this explicitly for dedicated research before Task 2.6 can close; a git-dependency reference (npm supports `git+https://...#<commit-sha>` with lockfile integrity) is the most plausible starting candidate given it needs no new registry infrastructure, but this is not a research-backed conclusion.

### Task 2.7 — Opus review

This report is the direct input to that review. Two things the reviewer should specifically probe, per the plan's own instruction: (1) whether the B.1 lift/adapt/drop classification is sound — the merged master table above resolves the three original batch tables plus the CDD deep-dive's overlapping rows without contradiction (see B.1's per-row cross-references), but a second pass on the borderline `adapt` calls (especially the ones flagged with a specific hardening requirement — `validate-routes-have-pages.js`'s `eval()` risk chief among them) is warranted; (2) whether the CDD-tier scope decisions in Part C §2.4 (always-on core / opt-in variant-shape split) match the plan's Decision 3/4 framing, which this report's B.2 findings substantiate but slightly sharpen (ec-site's own variant infrastructure is a single-component pilot, not a mature catalogue — Decision 4's choice to budget catalogue-building as real work is correct and now better-evidenced).

### Cross-cutting design notes for Phase 3 (build), flagged here so they aren't lost before that phase starts

- **CDD rule build order** (Task 3.3): adapt `no-html-in-pages.js` widened to cover both `src/pages/**` and `src/components/**` (no prose-context escape hatches needed here — `articles.ts` is exempted structurally per Decision 3, not via an in-JSX escape hatch); build `no-ad-hoc-classnames` using `enforce-width-standards.js`'s narrow-rule-with-exception-table shape as a *pattern* reference plus a broadened `eslint-plugin-tailwindcss` `no-custom-classname` scope; author variant-API enforcement from scratch (no ec-site rule exists to adapt) informed by `no-hard-coded-component-data.js`'s heuristic-classifier technique, targeting the CVA + `tailwind-merge` + `clsx` dependency trio already validated by ec-site's single Button.tsx example.
- **Always-on core-tier candidates** (independent of CDD), all TRANSFERS-DIRECTLY or lift-as-is per B.1: `no-eslint-disable.js` (three independent cross-project implementations of "don't suppress, fix" — strongest possible signal), `no-duplicate-section-ids.js`, `no-placeholder.js`, `require-explicit-type-annotations.js`, `require-exported-component-types.js`, `ssr-safe-hooks.js`/`no-browser-globals-in-render.js` (union, since this repo is also SSG), `validate-lazy-imports.js`, and the meta-rules (`no-async-handlers.js` etc., B.4).
- **Avoid rule-sprawl duplication**: don't replicate the `enforce-external-link-whitelist.js`/`no-external-links-in-pages.js` two-rules-one-policy pattern found in ec-site.
- **Safety hardening required before any adaptation**: `validate-routes-have-pages.js` must have its `eval()`-based extraction replaced with AST-based parsing before it is fit for a publishable package.
- **Governance/suppression-comment language**: reuse `no-orphaned-grid-items.js`'s "🤖 LLM Agents may suggest, 👤 humans authorise" phrasing verbatim for any CDD suppression-comment policy.
- **Documentation** (Task 3.5): follow A.7's structure exactly — README index → `docs/` topic files → `docs/tools/*.md` per-tool deep dives, real-file links with line anchors over restated interfaces, worked cascade examples over abstract descriptions.
- **CI template** (informs Task 4.6): the A.8 autofix-then-gate inline-barrier pattern is the target shape for `ts-qa-ci`'s own shipped GitHub Actions template, adapted for the simpler TS case where Prettier/ESLint's default mode is already read-only (no dry-run-flag plumbing needed the way PHP CS Fixer/Rector require) — but the phase-ordered orchestrator itself, and CI auto-detection, are net-new builds with no existing script to extend in this repo.
- **Licensing gate** (B.5): no ec-site code — rules, cache library, orchestrator scripts, or meta-rules — should be lifted into the package until the maintainer's sign-off described in B.5 is obtained. This is a blocking precondition for Task 3.1 onward, not merely a footnote.

---

## Appendix: Sources consulted (by originating research batch)

- **`php-qa-ci` pipeline architecture**: `bin/qa`; `README.md`; `CLAUDE.md`; `docs/pipeline.md`; `includes/functions.inc.bash`; `includes/options.inc.bash`; `includes/generic/{allCodingStandardsTools,rector,phpCsFixer,phpLint,phpstan,phpArkitect,phpunit,infection,composerRequireChecker,markdownLinks,composerChecks,phpStrictTypes,phploc}.inc.bash`.
- **Read-only/CI duality**: `includes/functions.inc.bash:252-289` (`detectReadOnly()`); `bin/qa:75-109`; `includes/generic/rector.inc.bash`; `includes/generic/phpCsFixer.inc.bash`; `/workspace/package.json`; `/workspace/.github/workflows/ci.yml`.
- **Tool delivery/encapsulation**: `README.md`; `composer.json`; `phive.xml`; `tools/rector/composer.json`; `scripts/tool-install.bash`; `src/ComposerPlugin/PhpStanGuardPlugin.php`; `/workspace/eslint.config.js`; `/workspace/eslint-rules/`.
- **Configuration cascade**: `includes/functions.inc.bash`; `includes/generic/setConfig.inc.bash`; `includes/symfony/{setConfig,allLintingTools}.inc.bash`; `qaConfig/qaConfig.inc.bash`; `docs/configuration.md`; `docs/platform-detection.md`.
- **Hook system / Claude Code integration**: `CLAUDE.md` (Hook System, Claude Code Hooks); `README.md` (Claude Code Integration); `scripts/deploy-skills.bash`; `src/ComposerPlugin/SkillsDeployPlugin.php`; `bin/qa`.
- **Rule-tiering philosophy**: `README.md` (PHPArkitect, Custom PHPStan Rules sections); `CLAUDE.md`; `includes/generic/phpArkitect.inc.bash`; `configDefaults/generic/phparkitect.php`; `/workspace/untracked/ec-site/eslint-rules/{no-html-in-pages,no-raw-block-html-in-articles}.md`; `/workspace/untracked/ec-site/CLAUDE.md`.
- **Documentation structure & CI patterns**: `docs/{pipeline,configuration,github-actions}.md`; `docs/tools/phpstan.md`; `templates/github-actions/{php-qa-ci,qa-autofix}.yml`; `.github/workflows/{ci,qa,update-deps}.yml`; `/workspace/.github/workflows/ci.yml`; `/workspace/README-QA.md`.
- **ec-site ESLint rules (batches 1–3, 54 rules)**: all `.js`/`.md` pairs under `/workspace/untracked/ec-site/eslint-rules/`, per the B.1 master table.
- **ec-site CDD deep-dive**: `no-html-in-pages.{js,md}`, `no-raw-block-html-in-articles.{js,md}`, `enforce-width-standards.{js,md}`, `no-orphaned-grid-items.{js,md}`, `no-hard-coded-component-data.{js,md}`, `no-duplicate-section-ids.{js,md}`; `/workspace/untracked/ec-site/eslint.config.js`; `/workspace/untracked/ec-site/package.json`; `/workspace/untracked/ec-site/src/components/ui/button.tsx`; `/workspace/untracked/ec-site/src/components/CLAUDE.md`.
- **ec-site orchestrator & meta-rules**: `scripts/qa.ts`; `package.json` (`llm:qa` and sibling scripts); `scripts/llm-*.ts`; `scripts/lib/cache.ts`; `scripts/lib/llm-output.ts`; `eslint-rules/meta-rules/{no-async-handlers,require-documentation,validate-message-ids}.js` + `README.md`; `eslint-rules/CLAUDE.md`.
- **This repo's own config, cross-referenced throughout**: `/workspace/CLAUDE.md`; this session's hooks-daemon `<hooksdaemon>` guidance (article-snippet-enforcer, error_hiding_blocker, hook_registration_checker, plan_qa_sweep); `/workspace/CLAUDE/Plan/011-ts-qa-ci-package/PLAN.md`.
